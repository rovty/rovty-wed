# Platform identity rollout

The coordinated architecture, migrations, credentials and staging guide lives
in [the dashboard repository](../../rovty-dashboard/docs/platform-identity.md).
Read that guide before deploying this release. Both Supabase projects must
receive their own migrations; no database merge or data move is needed.
