// Photo served from GitHub — swap the file in emiresh/wedrsvp to update without rebuilding.
// The rest of this wedding's data (names, date, venue) now comes from the
// `weddings` table via @/lib/wedding — this is the one field with no DB
// column yet (no thank-you-photo upload in the product), so it stays here.
export const weddingPhoto =
  "https://raw.githubusercontent.com/emiresh/wedrsvp/main/wedding-photo.jpg";
